import Styled from "styled-components";

const imageItem = Styled.div`
    padding: 0;
    width: 100%;
    overflow: hidden;
    margin-bottom: 10px;
    position: relative;
    opacity: 1;
`;

export default imageItem;
