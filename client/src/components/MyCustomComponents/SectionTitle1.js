import styled from "styled-components";
// import PropTypes from 'prop-types';

const SectionTitle1 = styled.h5`
  color: #021533;
  font-weight: 500;
  padding-left: 0px;
  padding-top: 10px;
  padding-bottom: 0px;
  box-sizing: border-box;
  vertical-align: center;
  line-height: 0.2;
`;

// Divider.prototype = {
//   url: PropTypes.string.isRequired,
// };

export default SectionTitle1;
